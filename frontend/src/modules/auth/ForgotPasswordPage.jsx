import { useState } from "react";
import { Link } from "react-router-dom";
import { apiRequest } from "../../utils/api";
export default function ForgotPasswordPage(){const[email,setEmail]=useState("");const[message,setMessage]=useState("");const submit=async(e)=>{e.preventDefault();const r=await apiRequest("/auth/forgot-password",{method:"POST",body:JSON.stringify({email})});setMessage(r.message)};return <main className="simple-auth-page"><section><Link to="/" className="auth-brand dark"><span>O</span><strong>Obsidian Mesa</strong></Link><span className="auth-kicker">RECUPERAR ACCESO</span><h1>Restablece tu contraseña</h1><p>Te enviaremos las instrucciones a tu correo.</p><form className="auth-form" onSubmit={submit}><label>Correo electrónico<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label>{message&&<div className="confirmation-box"><p>{message}</p></div>}<button className="auth-submit">Enviar instrucciones <span>→</span></button><Link className="back-link" to="/login">Volver al inicio</Link></form></section></main>}
